
/* export const validate = (input) => {
  let errors = {}

  if(!form.name) {
    errors.name = 'El nombre es requerido'
  } else if(!/^[a-zA-Z0-9-() .]+$/.test(input.name)){
    errors.name = 'Solo se aceptan letras, numeros, guiones medios y parentesis'
  }

  if(!form.apellido) {
    errors.apellido = 'El apellido es requerido'
  } else if(!/^[a-zA-Z0-9-() .]+$/.test(input.name)){
    errors.apellido = 'Solo se aceptan letras, numeros, guiones medios y parentesis'
  }

 /*  if(form.image.length !== 0 && !/^(https?|chrome):\/\/[^\s$.?#].[^\s]*$/.test(input.image)){
    errors.image='invalid URL'
  } */

  /* if(!form.aptitudes) {
    errors.aptitudes = 'Las aptitudes son requeridas'
  } else if (input.aptitudes.length > 100) {
    errors.aptitudes = 'Las aptitudes son demasiadas'
  }

  if(!form.edad) {
    errors.edad = 'La edad es requerida'
  }



  return errors;
}


 */ 